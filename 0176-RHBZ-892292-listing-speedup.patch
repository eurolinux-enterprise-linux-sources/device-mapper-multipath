---
 multipath/main.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: multipath-tools/multipath/main.c
===================================================================
--- multipath-tools.orig/multipath/main.c
+++ multipath-tools/multipath/main.c
@@ -312,7 +312,7 @@ configure (void)
 	/*
 	 * get a path list
 	 */
-	if (conf->dev)
+	if (conf->dev && !conf->list)
 		di_flag = DI_WWID;
 
 	if (conf->list > 1)
